@{
    ViewData["Title"] = "Home Page";
}
<script src="~/lib/jquery/dist/jquery.js"></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

<table id="tweets" class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Stamp
            </th>
            <th>
                Text
            </th>
        </tr>
    </thead>
    <tbody id="tweetBody"></tbody>
</table>

<script>
    $(document).ready(function () {
        $.get("/api/TweeterIO/GetTweetes", { startDateStr: "2016-01-01T00:00:00.00Z", endDateStr: "2017-12-31T00:00:00.00Z" })
            .done(function (tweetList) {
                var strResult = "";
                $.each(tweetList, function (index, tweets) {
                    $.each(tweets, function (index1, tweet) {
                        strResult += "<tr><td>" + tweet.id + "</td><td> " + tweet.stamp + "</td><td>" + tweet.text + "</td></tr>";
                    });
                });
                strResult += "";
                $("#tweetBody").html(strResult);
            });
    });
</script>